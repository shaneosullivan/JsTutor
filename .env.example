# OpenAI Integration (Required for AI assistance)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Database Configuration (Optional - uses in-memory storage by default)
# Only needed for persistent data storage in production
DATABASE_URL=postgresql://username:password@localhost:5432/jsadventure
PGHOST=localhost
PGPORT=5432
PGUSER=your_username
PGPASSWORD=your_password
PGDATABASE=jsadventure

# Application Settings
NODE_ENV=development
PORT=5000