---
version: 1
---

```javascript
// Let's do some maths and draw with it!
let x = 100;
let y = 150;
let size = 10 + 20; // This equals 30!

drawRect(x, y, size, size, "red");
drawRect(x + 50, y, size * 2, size, "green");
drawRect(x + 150, y, size / 2, size, "blue");
```
